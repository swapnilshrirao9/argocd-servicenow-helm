apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "servicenow.fullname" . }}-test-connection"
  labels:
    {{- include "servicenow.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "servicenow.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
